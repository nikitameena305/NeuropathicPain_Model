# Superseded passive export

The passive physiological gates passed, but the spatial rounding used for this
export allowed a maximum segment electrotonic fraction of 0.1085 at configured
`d_lambda = 0.1`. The directory is retained for provenance and must not be used
as the final passive acceptance result.

The strict-d-lambda export is stored in `../final_strict_dlambda/`.
